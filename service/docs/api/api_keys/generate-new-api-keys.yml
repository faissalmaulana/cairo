info:
  name: generate-new-api-keys
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/{{prefix_api_v1}}/account/apikeys"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

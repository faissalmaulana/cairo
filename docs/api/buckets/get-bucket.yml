info:
  name: get-bucket
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/{{prefix_api_v1}}/accounts/:account_id/buckets/project3"
  params:
    - name: account_id
      value: "{{account_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: delete-object
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/{{prefix_api_v1}}/accounts/:account_id/buckets/project1/objects/collection2/island.jpg"
  params:
    - name: account_id
      value: "{{account_id}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

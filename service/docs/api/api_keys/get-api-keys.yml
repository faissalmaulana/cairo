info:
  name: get-api-keys
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/{{prefix_api_v1}}/account/apikeys"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Get Authenticated User
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/{{prefix_api_v1}}/account/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

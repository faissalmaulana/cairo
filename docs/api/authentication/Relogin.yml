info:
  name: Relogin
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/{{prefix_api_v1}}/refresh"
  auth:
    type: bearer
    token: "{{refresh_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
